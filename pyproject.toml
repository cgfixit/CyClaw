[project]
name = "cyclaw"
version = "1.4.2"
description = "Offline-first RAG soul-governed LangGraph agent with governance invariants"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.10.dev133",
    # other deps...
]
