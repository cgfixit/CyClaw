# ... keep header
chromadb==1.5.10.dev133             # CVE-2026-45829 (pre-auth RCE via malicious model + trust_remote_code). Mitigated in CyClaw: local-only, code-enforced no remote code trust. See SECURITY_REVIEW_STATUS.md
# ... other lines