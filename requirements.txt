# CyClaw requirements (Python 3.12 verified, CVE mitigation via code)
fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pyyaml==6.0.3
httpx==0.28.1
langgraph==1.2.5
langchain-core==1.4.7
chromadb==1.5.6  # CVE-2026-45829 (Critical pre-auth RCE; no upstream patch). Risk accepted for local/offline CyClaw (localhost client binding + controlled embedding config). pip-audit ignore per threat model.
sentence-transformers==5.6.0
rank-bm25==0.2.2
numpy==1.26.4
nltk==3.9.4
pytest==9.1.0
pytest-asyncio==1.4.0
psycopg[binary]>=3.2.0    # optional: Postgres backend (set CYCLAW_DB_URL=postgresql://...)
