# CyClaw Setup Guide

The **canonical** install guide is the single full copy at the repository
root:

[`setup-guide.md`](../../setup-guide.md)

(`docs/work/SETUP.md` also redirects there.) This file is a pointer only —
not a second copy — so the two paths cannot drift.
