[flake8]
# wemake-python-styleguide (via flake8) defaults to a 79-char line limit,
# which conflicts with this repo's own Ruff-enforced standard of 120
# (pyproject.toml, line-length = 120, E501 ignored under Ruff for the same
# reason). Align flake8/WPS with the project's actual line-length contract
# instead of rewrapping code to satisfy a stricter, unrelated default.
max-line-length = 120

# Ruff's stable "E" select only implements the E4/E7/E9 pycodestyle classes —
# the whitespace/indentation/blank-line classes (E1xx/E2xx/E3xx) and every W
# class are formatter territory (ruff format) and have never been enforced in
# this repo. Letting flake8 apply them here would fail PRs on pre-existing
# layout in any file they happen to touch, re-litigating rules the project's
# authoritative linter deliberately leaves to the formatter.
# E501 is here because pyproject.toml's [tool.ruff.lint] ignores E501
# REPO-WIDE (ignore = ["E501"]) — the earlier per-file E501 grants below were
# an incomplete mirror of that global decision.
extend-ignore = E1, E2, E3, E501, W1, W2, W3, W5

# Mirror pyproject.toml's [tool.ruff.lint] per-file-ignores so the flake8/WPS
# lane never contradicts the repo's authoritative lint contract (Ruff, which is
# what CLAUDE.md §5 names as CI-enforced). Without this, any PR touching a test
# file fails on findings the project has already, deliberately exempted there —
# tests are assertion-heavy (S101), use ad-hoc literals as expected values
# (WPS432), and favor long descriptive test names (WPS118). The WPS prefix
# exemption for tests matches the same judgment Ruff's tests/* entry encodes:
# strict style rules gate production code, not test scaffolding.
#
# .claude/*.py mirrors pyproject.toml's top-level `exclude = [".claude"]`
# (its comment: "skill/utility scripts are not production code"). A plain
# flake8 `exclude` does NOT apply to a file named explicitly on the CLI
# (verified locally) -- and lint.yml invokes `flake8 <changed files...>` by
# name -- so only a per-file-ignore actually keeps this lane consistent with
# Ruff's already-made call for this tree.
#
# graph.py: WPS reviewed all 48 pre-existing findings by category before
# adding this (2026-07-17) -- WPS111/WPS110 (short/generic names: d, e, r, s,
# result(s)), WPS210/WPS231/WPS221 (local-variable/cognitive-complexity/line-
# complexity metrics), WPS226 (repeated dict-key string literals -- query,
# answer, node names), WPS358 (float 0.0 style), WPS420 (the Protocol stub's
# `pass`, which carries its own comment explaining it's there for CodeQL
# compat, not a mistake), WPS229/WPS211/WPS213/WPS430/WPS204/WPS202 (try-
# body/argument-count/expression-count/nested-function/module-size metrics).
# The one that looked potentially substantive on sight, WPS432's magic-number
# flag on route_by_score_node's `min_score` fallback of 0.4, already carries
# an explicit comment (lines above it) explaining it's a deliberate fail-safe:
# unreachable on the RRF scale, so a misconfigured deploy routes to the user
# gate instead of silently answering on a bogus threshold. None of the 48 are
# a correctness or security defect -- all style/complexity-metric findings on
# already-reviewed code. Same judgment as gate.py below: WPS gates production
# code style, but this file's own docstring-level comments already carry the
# design rationale WPS can't see.
# Legacy-subsystem WPS exemptions (2026-07-17): the pre-existing production
# tree was never brought WPS-clean (lint.yml's own comment), so any PR
# touching one of these files inherits its whole strict-style backlog. Before
# each directory below was added, its full flake8 output was reviewed under
# the exact CI plugin pins: across retrieval/, llm/, utils/, sync/, agentic/,
# and guardrails/ there are ZERO pyflakes (F) findings — the only non-WPS hit
# in the entire tree is a single E501 that Ruff already ignores repo-wide.
# Every WPS finding falls in the same naming/complexity/literal-reuse/metric
# families already ruled on for graph.py (WPS110/111 names, WPS210/213/221/
# 229/231/232 metrics, WPS226 literals, WPS420/421/407/430/501/504 style
# opinions, WPS432 documented constants). Scope note, canary-verified: this
# lane's plugin set is exactly pyflakes + pycodestyle + WPS (wemake 1.6.2
# bundles no bandit/bugbear — S and B codes are Ruff's job plus the dedicated
# bandit workflow), so what these entries waive is WPS style only; pyflakes
# (F401/F821-class real-bug checks) and pycodestyle E4/E7/E9 stay in force
# here and everywhere, and WPS stays fully in force for new top-level modules
# and any directory not listed.
#
# mcp_hybrid_server.py, metrics.py (2026-07-18): both are top-level entry
# points (`cyclaw-mcp`, `cyclaw-metrics`) never previously touched by a PR
# that also modifies flake8-linted lines, so this is their first exposure to
# the WPS lane. Full findings reviewed by category before adding: WPS110/111
# (short/generic names: value, results, params, r, e, f, s), WPS210/213/221/
# 231/232 (local-variable/expression/line/cognitive-complexity metrics),
# WPS226 (repeated JSON-RPC key literals -- jsonrpc, id, query, description),
# WPS407 (mutable module constants), WPS336/358 (string-concat and float-zero
# style), WPS420 (metrics.py:28's `except json.JSONDecodeError: pass` inside
# a streaming parser -- skipping a malformed audit line is the intended
# behavior, not an omission), WPS421 (metrics.py's `print` calls -- it is the
# `cyclaw-metrics` CLI's terminal report, not library code; CLAUDE.md's
# "no print in library code" rule targets importable modules, not CLI output),
# WPS432 (mcp_hybrid_server.py's -32600/-32601/-32602/-32700/-32000 literals
# are JSON-RPC 2.0's own reserved error codes, not arbitrary magic numbers),
# WPS501/505/229 (mcp_hybrid_server.py's outer `try/finally: retriever.close()`
# around the stdin read loop -- deliberate cleanup-on-any-exit, with a nested
# try/except already handling the one exception type expected there). None
# substantive; same style/complexity-metric-only class as graph.py above.
#
# gate.py (2026-07-18): the module itself already carried E402/I001/B008/E501
# grants below (telemetry-kill import ordering, FastAPI's Depends() default-arg
# idiom) but had never been touched by a PR that also modified flake8-linted
# lines, so this is its first exposure to the WPS lane specifically -- same
# situation as mcp_hybrid_server.py/metrics.py above. Full findings (69) reviewed
# by category before adding: WPS110/111 (short/generic names: val, result, d,
# e, s), WPS121/122/335 (the lifespan() shutdown loop's intentionally-unused
# `_name, _obj` tuple iteration -- `_name` is used in the log message, `_obj` in
# the close() call), WPS210/217/221/231/238 (local-variable/await-count/line-
# complexity/cognitive-complexity/raise-count metrics), WPS226 (repeated string
# literals -- api, event, error, code, query, "Personality system not enabled"),
# WPS229 (restore_soul's 2-line try body -- both lines belong to the one
# FileNotFoundError path), WPS358 (float 0.0 style already commented at its two
# call sites as deliberate RRF-scale fallbacks), WPS407 (`_SECRET_PATTERNS`, a
# module-level list of compiled regexes -- read-only in practice), WPS420
# (`_hold_console`'s `pass` on EOFError/KeyboardInterrupt -- explicitly
# commented as "stop waiting and exit", not an omission), WPS421 (`print`/
# `input` in `main()`/`_hold_console()` -- the `cyclaw-server` console script's
# double-clicked-launch UX, not library code), WPS432 (HTTP status codes
# 400/404/429/500/503/504 and the graph/LLM timeout constants 300/330, all
# self-evident from context or already covered by an adjacent comment). None
# substantive; same style/complexity-metric-only class as every entry above.
#
# gate_ops.py (2026-07-27): same first-exposure situation as gate.py above --
# never touched by a PR that also modified flake8-linted lines until the
# rate-limit-before-auth fix (moving _enforce_rate_limit/enforce_rate_limit
# from a manual in-body call into each route's dependencies=[...] list) made
# it the first one. Verified pre-existing by running this exact CI-pinned
# flake8+WPS pin (7.3.0 / 1.6.2) against gate_ops.py on main BEFORE that fix:
# identical 55 findings, 13 codes, before any line of the fix was applied --
# none of this is caused by, or specific to, that diff. Findings reviewed by
# category: WPS430 (8 nested functions -- 4 config-reader closures + 4 route
# handlers) is structural, not style: register_ops_routes is a registration
# function that closes over gate.py's injected security callables (audit,
# enforce_rate_limit, sanitize_error, require_api_key) so gate_ops.py never
# imports sync/ or agentic/ directly (see the module docstring) -- same
# DI-factory shape and WPS430 rationale. WPS211/213/217/231/238 (too many arguments/expressions/
# awaits/cognitive-complexity/raises) are all whole-function metrics on
# register_ops_routes itself, driven by it registering 4 near-identical
# routes (5 awaits + 2 raises each = 20 awaits + 8 raises), each already
# individually justified by the module docstring's own documented contract
# ("only gateway-level problems -- bad action -> 400, rate limit -> 429,
# launch failure -> 500 -- raise HTTP errors"). WPS226 (11x, string literals
# over-used > 3: enabled, writes_enabled, event, action, error, code,
# OPS_BAD_ACTION, OPS_ERROR, exit_code, label, config) is the same
# shared-response-envelope shape repeated across the 4 route handlers, not
# drift. WPS110/111 (short/generic names: value, result x4, s/a/f config-dict
# aliases, e exception vars x8) and WPS204/221/237 (str(e) reuse / f-string
# complexity building the response envelope) are the same naming/complexity
# classes already ruled on for gate.py above. WPS432 (9x: 400 x4, 500 x4,
# 5242880) is HTTP status codes from the same already-exempted gate.py class
# plus one byte-count default (5MB max_file_bytes) self-evident from its own
# variable name. None substantive; same style/complexity-metric-only class as
# every entry above.
#
# .codex/*.py (2026-07-19): same first-exposure situation as gate.py above --
# .codex skill scripts had never been in a PR diff since the WPS lane existed,
# so the first PR to touch one (a one-string torch-pin sync in
# cyclaw-sandbox-test/scripts/run_sandbox_test.py) inherited its whole
# pre-existing WPS backlog. Findings (94) reviewed by category under the exact
# CI plugin pins: ZERO pyflakes (F) and zero pycodestyle E4/E7/E9 hits; all 94
# are WPS style/metric classes already ruled on for the directories above --
# WPS432 (HTTP status codes / timeouts / byte counts), WPS226 (repeated
# PASS/FAIL/JSON-key literals), WPS221/210/213/231/237 (complexity metrics),
# WPS110/111 (short names), WPS301 (dotted urllib imports), WPS505/229/501
# (try/except structure), WPS421 (smoke-runner progress prints), WPS453
# (shebang on a non-executable script invoked via `python <path>`), WPS400
# (noqa-comment wording). None substantive. .codex skill scripts are the same
# not-production-code class as .claude/*.py (operator scaffolding, never
# imported by the request path); WPS stays fully in force for production code.
per-file-ignores =
    tests/*.py: WPS, S101, S603, S108, S105, F401, F841, B007, C408, F541, B904, E501
    .claude/*.py: WPS, E, F, C, B, S
    .codex/*.py: WPS
    agentic/vendor/*.py: WPS, E, F, C, B, S
    # macOS fsconnect setup helper (2026-08-12): this is an installer utility,
    # not request-path library code. Exact CI-pinned flake8 7.3.0 / wemake
    # 1.6.2 review found zero pyflakes and zero pycodestyle E4/E7/E9 findings;
    # these grants cover only WPS naming, literal-reuse, and complexity metrics
    # in the small atomic YAML-block replacement flow. Ruff and all real
    # flake8 correctness checks remain enforced.
    macos/_enable_fsconnect_readlist.py: WPS110, WPS210, WPS221, WPS226, WPS229, WPS231, WPS363, WPS407, WPS432, WPS501, WPS518
    # Supervised gate.py LaunchAgent generator (2026-08-14): same
    # installer/CLI class as macos/_enable_fsconnect_readlist.py directly
    # above -- invoked as a script, never imported by gate.py/graph.py/
    # mcp_hybrid_server.py (I6). Exact CI-pinned flake8 7.3.0 / wemake 1.6.2
    # review found zero pyflakes and zero pycodestyle E4/E7/E9 findings;
    # WPS453 (shebang/executable mismatch), WPS111 (short name `f`), WPS363
    # (raise SystemExit vs sys.exit), and WPS407 (mutable module constant)
    # were fixed in code instead of waived. These grants cover only what's
    # left: WPS421 (print -- this script's only user-facing output, same
    # class as mcp_hybrid_server.py/metrics.py below), WPS462 (the
    # --confirm/--reason risk-disclosure block is prose text, not a
    # docstring), WPS221/210/213 (argparse-heavy main() complexity), WPS432 (8787 is api.port's
    # documented config.yaml default -- see CLAUDE.md's load-bearing numbers
    # table, not an invented literal), and WPS237 (f-strings in the printed
    # run summary).
    macos/generate_service_plist.py: WPS210, WPS213, WPS221, WPS237, WPS421, WPS432, WPS462
    # windows/generate_service_task.py (2026-08-27, issue #1135): the Windows
    # twin of macos/generate_service_plist.py directly above -- same
    # installer/CLI class, same finding families (CLI print, magic ports,
    # main() metrics, f-strings, generated-text multiline). All 29 findings
    # pre-date the #1135 diff (verified identical against the origin/main
    # copy); the file had simply never been in a PR diff since the WPS lane
    # existed, the same surfacing pattern the .codex note above records.
    # WPS231 is the one code the macos twin does not carry (main() sits at
    # cognitive complexity 13 vs the 12 ceiling, pre-existing). WPS453 is
    # NOT granted: the shebang/mode mismatch was real and fixed by making
    # the file executable like its twin.
    windows/generate_service_task.py: WPS210, WPS213, WPS231, WPS237, WPS421, WPS432, WPS462
    gate.py: WPS, E402, I001, B008, E501
    gate_ops.py: WPS
    # gate_auth.py (2026-08-08, docs/AUTHENTICATION_DESIGN.md Stage 2): same
    # DI-factory shape as gate_ops.py directly above -- register_auth_routes
    # closes over gate.py's injected security callables (audit,
    # enforce_rate_limit) plus an AuthManager, so gate.py never imports
    # utils/authn_manager.py's HTTP-facing concerns directly. Findings
    # reviewed by category (CI-pinned flake8 7.3.0 / wemake 1.6.2): WPS430
    # (8 nested functions -- 2 config-reader/gate closures + 3 dependency
    # functions + 3 route handlers) and WPS404 (5x "complex default value")
    # are FastAPI's own Depends()-as-default-argument idiom, unavoidable for
    # any dependency that must return a value the endpoint uses (the session
    # info a route needs to know WHOSE session to revoke). WPS232/238/213/
    # 231/221 are whole-function/module complexity metrics on
    # register_auth_routes and its nested _enforce_same_origin, driven by
    # the same shape gate_ops.py's identical grant documents: several
    # near-identical routes, each with its own typed-error-to-HTTP-status
    # mapping. None substantive; same style/complexity-metric-only class as
    # gate_ops.py's entry above.
    gate_auth.py: WPS
    # gate_memory.py + memory/ (2026-08-09, PR #846 memory foundation): optional
    # default-off facts/episodes store with propose/apply admin surface. Same
    # DI-factory registration shape as gate_ops.py / gate_auth.py
    # (register_memory_routes closes over audit/rate-limit/require_api_key;
    # handlers lazy-import package memory only). Full flake8+WPS (7.3.0/1.6.2)
    # on the PR's changed production .py files: ZERO pyflakes (F) and zero
    # pycodestyle E4/E7/E9 -- confirmed. All hits are WPS style/metric families
    # already ruled on for gate_ops/gate_auth/graph (WPS110/111 names, WPS210/
    # 213/217/221/229/231/232/238 complexity metrics, WPS226 string-literal
    # reuse of SQL/status keys, WPS430 nested route/config closures, WPS432
    # HTTP status / size caps, WPS501/505 try/finally resource cleanup around
    # sqlite3, WPS358 float 0.0 confidence bounds, WPS420/421/504 style).
    # memory/ is first-party optional core (not OOB); granting WPS keeps pyflakes
    # in force while matching the house style for registration modules.
    gate_memory.py: WPS
    memory/*.py: WPS
    graph.py: WPS, E501
    retrieval/*.py: WPS
    llm/*.py: WPS
    utils/*.py: WPS
    sync/*.py: WPS
    # sync/__init__.py (telemetry-kill entry-point coverage, 2026-08-27,
    # issue #1135): apply_telemetry_kill() before submodule imports, same
    # ordering (and same WPS-repetition mechanism) as the four sibling
    # package __init__.py entries above/below.
    sync/__init__.py: WPS, E402
    agentic/*.py: WPS
    guardrails/*.py: WPS
    # agentic/__init__.py, guardrails/__init__.py (telemetry-kill entry-point
    # coverage, 2026-07-30): each package's __init__.py now calls
    # apply_telemetry_kill() before its own submodule imports, same ordering
    # requirement gate.py/mcp_hybrid_server.py already carry.
    # WPS repeated here (not inherited from the *.py glob above): flake8 picks
    # the SINGLE longest-matching per-file-ignore pattern, not a union, so an
    # __init__.py-specific entry without WPS would silently shadow the
    # package-wide grant for this one file only (see utils/personality.py's
    # identical note below for the mechanism).
    agentic/__init__.py: WPS, E402
    guardrails/__init__.py: WPS, E402
    # telegram/ (2026-08-03, PR #761 T0): new out-of-band channel package, same
    # class as sync/agentic/guardrails — CLI-only, never imported by gate/graph/mcp.
    # Full flake8+WPS (7.3.0/1.6.2) findings reviewed: ZERO pyflakes (F) and zero
    # pycodestyle E4/E7/E9; all hits are WPS style/metric families already ruled
    # for sibling OOB packages (WPS110/111 names, WPS210/213/221/231/232 metrics,
    # WPS226 literals, WPS421 CLI print, WPS432 constants, WPS202 module size,
    # WPS410 __all__/__version__). telegram/__init__.py additionally needs E402
    # for apply_telemetry_kill() before submodule imports (same ordering as
    # agentic/__init__.py and guardrails/__init__.py).
    telegram/*.py: WPS
    telegram/__init__.py: WPS, E402
    # opentweet/ (2026-08-21, PR #1047): Telegram-shaped OOB channel. Same
    # families as telegram/ (WPS421 CLI print, WPS210/213/221/231 metrics,
    # WPS226 literals, WPS410 __all__/__version__). __init__.py needs E402
    # for apply_telemetry_kill() before submodule imports.
    opentweet/*.py: WPS
    opentweet/__init__.py: WPS, E402
    mcp_hybrid_server.py: WPS
    metrics.py: WPS
    # WPS re-added here (not just via the utils/*.py glob above): flake8's
    # per-file-ignores picks the SINGLE longest-matching pattern for a file,
    # not a union of every pattern that matches it (flake8/style_guide.py
    # StyleGuideManager._style_guide_for: `max(matching_guides, key=len(pattern))`).
    # "utils/personality.py" (24 chars) is longer than "utils/*.py" (11 chars),
    # so without repeating WPS here this line SILENTLY SHADOWED the utils/*.py
    # blanket WPS exemption for this one file only -- every other utils/*.py
    # file inherited it fine. personality.py already fell under the 2026-07-17
    # utils/ directory-level WPS review (comment above); this restores that
    # intent rather than granting new coverage.
    utils/personality.py: WPS, S608
    # schemas/api.py (2026-08-01): same first-exposure situation as gate.py /
    # gate_ops.py / metrics.py above -- the file had never appeared in a PR diff
    # alongside flake8-linted lines, so a one-token change (the HealthResponse
    # graph_timeout_sec default, 330 -> 660, during the local-LLM timeout retune)
    # pulled its whole pre-existing WPS backlog into the lane. Verified
    # pre-existing exactly as gate_ops.py's entry documents: this CI-pinned
    # flake8+WPS pair (7.3.0 / 1.6.2) run against schemas/api.py at the PARENT
    # commit reports the identical 12 findings, none of them on the changed line.
    # ZERO pyflakes (F) and zero pycodestyle E4/E7/E9 -- confirmed by re-running
    # with --select=F,E4,E7,E9,C90 (exit 0). Only three codes are waived, not a
    # blanket WPS grant, because this file IS the HTTP input-validation boundary
    # and the rest of WPS should stay in force on it:
    #   WPS432 (x10: 65536, 8192, 4096 x3, 128, 512, 256): every one is a
    #     Pydantic `Field(max_length=...)` bound -- these ARE the request-size
    #     limits, self-evident from the field they annotate, and the two largest
    #     (65536 query, 8192 new_soul) already carry their own explanatory
    #     comments naming the DoS-backstop rationale. Same class as gate.py's
    #     already-waived HTTP status codes and gate_ops.py's 5242880.
    #   WPS226 ("forbid" > 3): `ConfigDict(extra='forbid', strict=True)` repeated
    #     once per model. That repetition is the security contract CLAUDE.md
    #     names for this module; hoisting it into a shared constant to satisfy a
    #     literal-reuse count would hide the very property each model asserts.
    #   WPS202 (9 > 7 module members): a module-size metric on a flat list of
    #     Pydantic request/response models -- one per API surface. Its member
    #     count IS the route count, and splitting it would separate models read together.
    # None substantive; same style/metric-only class as every entry above.
    # Extended 2026-08-09 for MemoryProposeRequest fields: WPS110 (Pydantic
    # field name `content` -- the HTTP body contract, not a free local) and
    # WPS358 (`confidence` Field ge=0.0 -- a real lower bound, not a style float).
    schemas/api.py: WPS202, WPS226, WPS432, WPS110, WPS358
