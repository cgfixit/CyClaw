# OSV-Scanner — scheduled & PR dependency vulnerability scanning.
#
# Scans the dependency manifests/lockfiles in this repo against the OSV.dev
# database and uploads native SARIF to the GitHub Security > Code scanning tab.
# Uses Google's pinned reusable workflows; every third-party ref is pinned to a
# full commit SHA (see the trailing "# vX.Y.Z" comments) for supply-chain safety.
name: OSV-Scanner

on:
  # Weekly full scan — Mondays 06:00 UTC (staggered one hour before pip-audit).
  # NOTE: scheduled triggers only fire from the repository's default branch.
  schedule:
    - cron: "0 6 * * 1"
  # Manual run from the Actions tab.
  workflow_dispatch:
  # Full scan on every push to the default branch.
  push:
    branches: [main]
  # Differential scan on PRs targeting main (gates new vulnerabilities).
  pull_request:
    branches: [main]

# Deny-all by default; each job grants only what it needs.
permissions: {}

# Cancel superseded in-progress runs on the same branch to save CI minutes;
# pushes to main are exempt so main keeps a complete scan history.
concurrency:
  group: osv-scanner-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # PR events: differential scan that flags vulnerabilities introduced by the PR.
  scan-pr:
    if: ${{ github.event_name == 'pull_request' }}
    permissions:
      security-events: write # upload SARIF to code scanning
      contents: read # checkout
      actions: read # required by the codeql/upload-sarif step
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@6e4298ebc4db23e847df9b2e2de2939d6f066c67 # v2.5.1
    with:
      # Use targeted ignore config + recurse from the repository root.
      # Keep strict on PRs so new vulns block merges.
      # Skip tools/lora_finetune (regex, not a bare dirname: v2.5.1's
      # exact-name exclude still walked this tree). Opt-in CUDA toolkit,
      # not a CyClaw runtime lockfile. OSV v2 resolves requirements.txt
      # transitives and Unsloth's published graph still expands to
      # known-vulnerable wheels (pillow 9.5, aiohttp 3.9.5, torch 2.9.1)
      # that this repo does not install. Operators pip-audit on the GPU
      # box; see the kit README.
      scan-args: |-
        --config=.osv-scanner.toml
        --experimental-exclude=r:lora_finetune
        -r
        ./
      fail-on-vuln: true

  # schedule / push / manual: full-tree scan, results published to the Security tab.
  scan-scheduled:
    if: ${{ github.event_name == 'schedule' || github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    permissions:
      security-events: write # upload SARIF to code scanning
      contents: read # checkout
      actions: read # required by the codeql/upload-sarif step
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@6e4298ebc4db23e847df9b2e2de2939d6f066c67 # v2.5.1
    with:
      # Use targeted ignore config + recurse from the repository root.
      # Non-blocking on scheduled runs (reports to Security tab without failing workflow).
      # Same lora_finetune exclude as scan-pr — opt-in GPU toolkit, not runtime.
      scan-args: |-
        --config=.osv-scanner.toml
        --experimental-exclude=r:lora_finetune
        -r
        ./
      fail-on-vuln: false
