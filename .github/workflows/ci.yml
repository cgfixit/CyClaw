# ==============================================================================
# CyClaw CI — Tests + Coverage + Reproducible Install Gate
# v2 hardening (June 2026):
#   - Exact pip pin ==26.1.2, not floating >=, across every job that upgrades it
#     (eliminates 4 pip CVEs in CI env per #71 analysis; exact pin keeps CI
#     reproducible rather than silently picking up whatever pip ships next)
#   - Explicit hermetic prepare (mkdirs, minimal soul.md, env var) cross-platform
#   - Real RAG smoke validates ChromaDB (version pinned in constraints.txt) + BM25 + RRF on committed corpus
#   - Coverage scoped to exercised modules only
#   - workflow-lint (actionlint + zizmor) and dependency-review added as
#     fast, no-heavy-deps first gates — this file's own YAML and any new
#     PR-introduced dependency get checked before the expensive matrix runs
#   - ollama-mock-smoke: a real-socket HTTP-server test of LocalLLMClient
#     against the OpenAI-compatible endpoint, complementing (not replacing)
#     tests/test_client.py's monkeypatched error-mapping/retry coverage
#   - real-repo-run-smoke (2026-08, docs/agentic/DEFERRED_WORK.md D1): a
#     real-socket, real subprocess (deterministic fake gh), real local-git-clone smoke of the
#     real-repo-run wiring, closing a gap the existing CLI contract tests
#     leave open by mocking every outer boundary in Python
#   - Aligns with v1.9.0 invariants (reproducible CI security gate, Python 3.12, stable Chroma pin)
#   - Cross-ref: cyclaw-advisor + python-development best practices for observable, testable CI
# ==============================================================================

name: CyClaw CI (Tests + Coverage + Gate)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Cancel superseded runs: when new commits are pushed to the same PR/branch,
# in-progress runs for that ref are cancelled instead of running to completion
# on stale code. Pushes to main are exempt (cancel-in-progress is false there)
# so every commit landed on main keeps full, uninterrupted CI history.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  docker-build:
    name: docker-build (Dockerfile sanity)
    runs-on: ubuntu-latest
    timeout-minutes: 18
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      # Docs-only PRs cannot change the image. Do not skip .claude/: Dockerfile
      # COPY . . and .dockerignore does not exclude that tree, so skill scripts
      # land in the image.
      # Push/schedule still always builds (EVENT_NAME != pull_request).
      - name: Detect image-relevant changes
        id: changes
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          if [ "$EVENT_NAME" != "pull_request" ]; then
            echo "docker_changed=true" >> "$GITHUB_OUTPUT"
          else
            non_docs=$(git diff --name-only "origin/${BASE_REF}...HEAD" | grep -vE '^docs/' || true)
            if [ -n "$non_docs" ]; then
              echo "docker_changed=true" >> "$GITHUB_OUTPUT"
            else
              echo "docker_changed=false" >> "$GITHUB_OUTPUT"
            fi
          fi

      - name: Set up Docker Buildx
        if: steps.changes.outputs.docker_changed == 'true'
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Build image (no push)
        if: steps.changes.outputs.docker_changed == 'true'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          platforms: linux/amd64
          cache-from: type=gha
          # cache-to omitted on purpose: mode=max on this torch-heavy image
          # flakes under GHA cache reservation ("failed to reserve cache")
          # and must not fail a pure Dockerfile sanity job. publish-ghcr.yml
          # owns the real cache + push surface.
  # Fast, no-heavy-deps gates that run before the expensive matrix below.
  # Neither needs torch/chromadb/sentence-transformers, so both finish in
  # well under a minute on a cache hit.
  workflow-lint:
    name: workflow-lint (actionlint + zizmor)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          # actionlint-py/zizmor have no manifest pin (installed ad hoc below), so
          # key the cache on the workflow file itself -- same pattern semgrep.yml
          # already uses for the identical situation.
          cache-dependency-path: .github/workflows/ci.yml

      - name: Install actionlint + zizmor (pinned, from PyPI -- not curl|bash)
        # actionlint-py's pip install fetches the prebuilt actionlint binary
        # from GitHub releases at build time and puts it on PATH as
        # `actionlint`; zizmor ships as a self-contained wheel, no fetch needed.
        run: python -m pip install actionlint-py==1.7.12.24 zizmor==1.28.0

      - name: Run actionlint
        # Lint every workflow under .github/workflows/, including the
        # workflow_dispatch no-op at environment.yml (relocated conda spec).
        run: actionlint -color .github/workflows/*.yml

      - name: Run zizmor (Actions security static analysis)
        # --gh-token enables the online audits (e.g. ref-version-mismatch's tag
        # lookup) that --offline would otherwise skip.
        #
        # --min-severity=high: an initial full run (offline + online) found 25
        # pre-existing medium/low findings scattered across ~10 files this PR
        # never touched (mostly missing persist-credentials: false on
        # read-only checkouts) -- fixing all of them needs per-job
        # verification that nothing downstream relies on the persisted
        # credential, which is real follow-up work, not something to block
        # every future PR on starting today. This still fails the build on
        # any HIGH-severity finding (the 2 that existed were already fixed
        # in this PR) -- run without the flag locally for the full picture.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: zizmor --gh-token "$GH_TOKEN" --min-severity=high .github/workflows/

  invariant-guard:
    name: invariant-guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'

      - name: Run invariant-guard (blocking)
        # Static AST check over gate.py/gate_ops.py/graph.py/mcp_hybrid_server.py
        # -- stdlib-only, no torch/chromadb install needed (PyYAML is optional;
        # the script degrades to a text scan for G3 when it's absent), so this
        # is a fast, non-flaky gate that belongs beside workflow-lint above,
        # not inside the verify-skills matrix below. That matrix also runs
        # invariant-guard's verify.sh, but the whole matrix job carries
        # `continue-on-error: true` for genuinely flaky app-runtime e2e smokes
        # (index build + live uvicorn) -- invariant-guard shared that
        # non-blocking umbrella by accident, not by design, and was never a
        # real gate on the six invariants it exists to enforce.
        run: python3 .claude/skills/invariant-guard/check_invariants.py

  packaging:
    # Validates the artifact a REAL USER installs -- the built wheel -- which
    # nothing else in CI touches. python-publish.yml builds it only on
    # `release: published` and uploads straight to PyPI with no install or
    # smoke test in between, and the 3-OS test matrix runs from the source
    # tree, so wheel packaging had zero coverage on any PR.
    #
    # That is not a theoretical gap: pyproject.toml's own
    # [tool.hatch.build.targets.wheel] comment records the exact incident --
    # gate.py and friends were absent from the wheel, so every console script
    # except the ones rooted in a directory package raised ModuleNotFoundError
    # on a real install. The same comment notes `pip install -e .` could not
    # have caught it (editable installs point at the repo root and never
    # consult the packaging list), which is why this job builds and inspects
    # the wheel itself rather than doing an editable install.
    #
    # Ubuntu-only and dependency-free by design: the wheel is pure Python, so
    # its contents do not vary by OS, and unzipping it answers "is the module
    # present at the path the entry point names" without installing torch.
    name: Wheel packaging (entry points resolvable)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: Build the wheel
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          # Same two tools python-publish.yml uses to build the released
          # artifact; keep them in step if that workflow's build stack changes.
          python -m pip install "build==1.6.0"
          python -m build --wheel

      - name: Every [project.scripts] entry point resolves inside the wheel
        run: |
          python - <<'PY'
          import sys, tomllib, zipfile
          from pathlib import Path

          pyproject = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          scripts = pyproject["project"]["scripts"]
          wheels = sorted(Path("dist").glob("*.whl"))
          if len(wheels) != 1:
              sys.exit(f"expected exactly one wheel in dist/, found {[w.name for w in wheels]}")
          names = set(zipfile.ZipFile(wheels[0]).namelist())

          missing = []
          for script, target in sorted(scripts.items()):
              # "pkg.mod:main" -> the module must ship as pkg/mod.py, and a
              # top-level "mod:main" as mod.py. A package entry point may also
              # be satisfied by pkg/mod/__init__.py.
              module = target.split(":", 1)[0]
              stem = module.replace(".", "/")
              if f"{stem}.py" in names or f"{stem}/__init__.py" in names:
                  print(f"OK   {script} -> {module}")
              else:
                  missing.append(f"{script} -> {module} (looked for {stem}.py, {stem}/__init__.py)")

          if missing:
              print(f"\nwheel: {wheels[0].name}", file=sys.stderr)
              for entry in missing:
                  print(f"MISSING {entry}", file=sys.stderr)
              sys.exit(
                  "\nThese console scripts would raise ModuleNotFoundError on a real "
                  "`pip install cyclaw`. Add the module to [tool.hatch.build.targets.wheel] "
                  "packages/force-include in pyproject.toml."
              )
          print(f"\nall {len(scripts)} entry points present in {wheels[0].name}")
          PY

  dependency-review:
    name: dependency-review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0
        with:
          fail-on-severity: high
          # chromadb's HTTP-server advisories are risk-accepted for CyClaw's
          # embedded PersistentClient (docs/THREAT_MODEL.md: "No HTTP DB").
          # Same set already ignored in .osv-scanner.toml and pip-audit.yml
          # (CVE-2026-45829/30/31/33). The action takes GHSA IDs only.
          # Every other high/critical advisory still fails this gate. See SECURITY.md.
          allow-ghsas: GHSA-f4j7-r4q5-qw2c,GHSA-2wm9-hf6c-p5cr,GHSA-xph7-9rjv-w5fr,GHSA-36p7-vc44-83pf

  windows-installer-smoke:
    name: windows-2022 / Windows PowerShell 5.1 installer
    runs-on: windows-2022
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Parse and exercise installer with Windows PowerShell 5.1
        # Keep this on the inbox `powershell` host, not pwsh: Install-CyClaw.ps1
        # explicitly supports Windows PowerShell 5.1. A UTF-8-without-BOM smart
        # punctuation regression previously parsed under pwsh 7 but failed here.
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $Installer = Join-Path $env:GITHUB_WORKSPACE "powershell\Install-CyClaw.ps1"
          $Uninstaller = Join-Path $env:GITHUB_WORKSPACE "powershell\Uninstall-CyClaw.ps1"
          $OriginalUserProfile = $env:USERPROFILE

          try {
            $tokens = $null
            $parseErrors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $Installer, [ref]$tokens, [ref]$parseErrors
            ) | Out-Null
            if ($parseErrors.Count -ne 0) {
              $parseErrors | ForEach-Object { Write-Error $_.Message }
              throw "Install-CyClaw.ps1 does not parse under Windows PowerShell 5.1"
            }

            $env:USERPROFILE = Join-Path $env:RUNNER_TEMP "cyclaw-installer-ps51"
            New-Item -ItemType Directory -Force -Path $env:USERPROFILE | Out-Null
            & $Installer -RepoPath $env:GITHUB_WORKSPACE -SkipPythonDeps -NoProfileEdit -NoPathEdit

            $HomeDir = Join-Path $env:USERPROFILE ".CyClaw"
            foreach ($relative in @("bin", "sessions", "skills", "tools", "memory")) {
              if (-not (Test-Path -LiteralPath (Join-Path $HomeDir $relative))) {
                throw "installer did not create .CyClaw\$relative"
              }
            }
            foreach ($relative in @("bin\Invoke-CyClaw.ps1", "bin\cyclaw.cmd")) {
              if (-not (Test-Path -LiteralPath (Join-Path $HomeDir $relative) -PathType Leaf)) {
                throw "installer did not create .CyClaw\$relative"
              }
            }
            $shim = Get-Content -LiteralPath (Join-Path $HomeDir "bin\cyclaw.cmd") -Raw
            if (-not $shim.Contains($env:GITHUB_WORKSPACE)) {
              throw "installer shim does not reference the PR checkout"
            }

            & $Uninstaller
            if (-not (Test-Path -LiteralPath $HomeDir -PathType Container)) {
              throw "default uninstall deleted .CyClaw data"
            }

            # Exercise the same constructible payload as the pwsh lane below,
            # this time through the claimed Windows PowerShell 5.1 runtime.
            $env:USERPROFILE = Join-Path $env:RUNNER_TEMP "cyclaw-installer-ps51-adversarial"
            New-Item -ItemType Directory -Force -Path $env:USERPROFILE | Out-Null
            $EvilDir = Join-Path $env:RUNNER_TEMP 'evil$(Get-Date)'
            Copy-Item -LiteralPath (Join-Path $env:GITHUB_WORKSPACE "gate.py") `
              -Destination (Join-Path $EvilDir "gate.py")

            $threw = $false
            try {
              & $Installer -RepoPath $EvilDir -SkipPythonDeps -NoProfileEdit -NoPathEdit
            }
            catch {
              $threw = $true
              if ($_.Exception.Message -notmatch "refusing") {
                throw "installer failed for an unexpected reason: $($_.Exception.Message)"
              }
            }
            if (-not $threw) {
              throw "installer accepted a repo path containing shell metacharacters"
            }
            if (Test-Path -LiteralPath (Join-Path $env:USERPROFILE ".CyClaw\bin\cyclaw.cmd")) {
              throw "installer wrote a shim after rejecting the adversarial path"
            }
          }
          finally {
            $env:USERPROFILE = $OriginalUserProfile
          }

  test:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # Bound every run: the install step pulls torch + chromadb + sentence-transformers
    # and the RAG smoke spins up an embedded ChromaDB. A hung download or a wedged
    # service should fail fast, not idle until GitHub's 6-hour default and burn
    # Actions minutes on all three OS legs of the matrix.
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        # macos-latest is GitHub's Apple Silicon (arm64) runner. Every step
        # except torch installation uses `shell: bash` uniformly across all
        # three OSes; torch gets its own macOS-specific install step (below)
        # because torch==2.13.0+cpu 404s on https://download.pytorch.org/whl/cpu
        # for macOS -- confirmed on this matrix leg's first run, and documented
        # in setup-guide.md.
        os: [ubuntu-latest, windows-latest, macos-latest]
    # All three supported hosts are release gates. Platform-specific skips are
    # reported below with `pytest -rs`; a failing Windows result is not masked.

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: macOS install-script smoke test (layout + shim + profile integration)
        if: runner.os == 'macOS'
        # Exercises macos/install-cyclaw.sh's and uninstall-cyclaw.sh's own shell
        # logic -- the one part of the macOS/Linux launcher with no CI coverage
        # at all (confirmed: no workflow file anywhere references install-cyclaw.sh
        # or uninstall-cyclaw.sh). Placed here, before "Setup Python 3.12", because
        # install-cyclaw.sh is self-contained -- it clones its own repo copy and
        # builds its own venv -- so it has zero dependency on anything below and
        # testing it first keeps it isolated from the rest of this job's Python
        # setup. --skip-python-deps keeps it fast (venv/pip correctness is already
        # covered by "Install deps (macOS)" below); the PATH/profile rc-file edits
        # are deliberately NOT skipped, since exercising them is the point.
        #
        # HOME is overridden to a throwaway dir for the whole step so this can never
        # touch the real runner home or any later step -- required even though
        # nothing downstream today sources a shell rc file, because
        # install-cyclaw.sh's detect_rc_file()/`touch "$RC_FILE"` runs
        # UNCONDITIONALLY, before the --no-path-edit/--no-profile-edit checks (which
        # this test doesn't pass anyway, since it needs those code paths to run).
        #
        shell: bash
        env:
          HOME: ${{ runner.temp }}/cyclaw-smoke-home
          SHELL: /bin/bash
        run: |
          set -euo pipefail
          mkdir -p "$HOME"
          echo 'export CYCLAW_PROFILE_SENTINEL=loaded' > "$HOME/profile-target"
          ln -s profile-target "$HOME/.profile"
          echo "[smoke] isolated HOME=$HOME"
          fail() { echo "[smoke] FAIL: $1" >&2; exit 1; }

          # --repo-path points the installer at THIS PR's own checkout instead of
          # cloning origin/main -- without it the smoke test would silently
          # validate main forever, never the branch under test.
          # The early smoke intentionally skips config enablement because PyYAML is
          # installed later in this job. tests/test_macos_fsconnect_setup.py runs
          # the default enablement path after dependencies are present.
          bash macos/install-cyclaw.sh --repo-path "$GITHUB_WORKSPACE" --skip-python-deps --no-fsconnect < /dev/null

          [ -d "$HOME/.CyClaw" ] || fail "home directory not created"
          # NOT "repo" -- that dir is only created on the clone path; --repo-path
          # points REPO_DIR straight at the external checkout instead (verified
          # against the script's own home-layout loop).
          for d in bin sessions skills tools memory; do
            [ -d "$HOME/.CyClaw/$d" ] || fail "missing \$HOME/.CyClaw/$d"
          done
          [ -x "$HOME/.CyClaw/bin/cyclaw" ] || fail "cyclaw shim missing or not executable"
          [ -d "$HOME/CyClaw-FS" ] || fail "fsconnect jail not prepared"
          [ "$(stat -f '%Lp' "$HOME/CyClaw-FS")" = "700" ] || fail "fsconnect jail mode is not 700"
          [ -f "$HOME/CyClaw-FS/README.txt" ] || fail "fsconnect jail README missing"
          grep -qF "$GITHUB_WORKSPACE" "$HOME/.CyClaw/bin/cyclaw" \
            || fail "shim does not reference the checked-out repo (--repo-path not honored)"

          # macOS bash login shells use the first existing file in
          # ~/.bash_profile, ~/.bash_login, ~/.profile order. Preserve and update
          # that file instead of creating ~/.bash_profile and suppressing it.
          RC_FILE="$HOME/.profile"
          RC_TARGET="$HOME/profile-target"
          [ ! -e "$HOME/.bash_profile" ] || fail "installer suppressed an existing ~/.profile"
          [ ! -e "$HOME/.bashrc" ] || fail "macOS bash install wrote the non-login ~/.bashrc"
          [ -L "$RC_FILE" ] || fail "installer replaced a symlinked ~/.profile"
          grep -qF "# >>> cyclaw harness path >>>" "$RC_TARGET" || fail "PATH block missing from ~/.profile target"
          grep -qF "# >>> cyclaw harness >>>" "$RC_TARGET" || fail "cyclaw() function block missing from ~/.profile target"
          LOGIN_TYPE="$(/bin/bash --login -c 'test "$CYCLAW_PROFILE_SENTINEL" = loaded && type -t cyclaw' || true)"
          [ "$LOGIN_TYPE" = "function" ] || fail "existing ~/.profile was not preserved and sourced"
          echo "[smoke] install-cyclaw.sh: layout + shim + profile integration OK"

          # Current main wrote clean macOS bash installs to ~/.bashrc. Seed that
          # legacy shape and prove uninstall removes both old and new hooks.
          cp "$RC_TARGET" "$HOME/.bashrc"
          chmod 600 "$RC_TARGET"
          # Nested and mixed marker damage must leave the whole file unchanged,
          # including an otherwise valid managed block in the same file.
          printf '%s\n' \
            'keep-before' \
            '# >>> cyclaw harness path >>>' \
            'managed-looking' \
            '# >>> cyclaw harness path >>>' \
            'USER_KEEP' \
            '# <<< cyclaw harness path <<<' \
            '# >>> cyclaw harness >>>' \
            'cyclaw() { :; }' \
            '# <<< cyclaw harness <<<' \
            'keep-after' > "$HOME/.bash_login"
          cp "$HOME/.bash_login" "$HOME/.bash_login.expected"
          bash macos/uninstall-cyclaw.sh < /dev/null
          [ -L "$RC_FILE" ] || fail "uninstall replaced a symlinked ~/.profile"
          grep -qF "# >>> cyclaw harness path >>>" "$RC_TARGET" && fail "PATH block survived uninstall"
          grep -qF "# >>> cyclaw harness >>>" "$RC_TARGET" && fail "cyclaw() block survived uninstall"
          grep -qF "CYCLAW_PROFILE_SENTINEL=loaded" "$RC_TARGET" || fail "uninstall changed user-owned profile content"
          [ "$(stat -f '%Lp' "$RC_TARGET")" = "600" ] || fail "uninstall changed ~/.profile target permissions"
          grep -qF "# >>> cyclaw harness path >>>" "$HOME/.bashrc" && fail "legacy ~/.bashrc PATH block survived uninstall"
          grep -qF "# >>> cyclaw harness >>>" "$HOME/.bashrc" && fail "legacy ~/.bashrc function block survived uninstall"
          cmp -s "$HOME/.bash_login" "$HOME/.bash_login.expected" || fail "uninstall partially edited a malformed rc file"
          [ -d "$HOME/.CyClaw" ] || fail "uninstall must keep ~/.CyClaw by default (no --remove-home passed)"
          echo "[smoke] uninstall-cyclaw.sh: profile blocks removed, data kept OK"

      - name: Windows install-script adversarial -RepoPath smoke test
        if: runner.os == 'Windows'
        # Regression test for a confirmed command-injection finding
        # (docs/audits/2026-08-02-cyclaw-sandbox-macos-harness-and-guardrails.md,
        # finding 2 -- previously unverified, now confirmed via a local
        # reproduction with a real pwsh runtime): powershell/Install-CyClaw.ps1
        # writes the resolved -RepoPath into $PROFILE.CurrentUserAllHosts via a
        # here-string, unescaped, inside a double-quoted `$env:CYCLAW_REPO =
        # "..."` assignment. PowerShell double-quoted strings evaluate a
        # literal $(...) subexpression inline, so a -RepoPath directory name
        # containing $(...) becomes live code the next time any PowerShell
        # session sources the generated profile. NTFS does not forbid $, (,
        # ), or ` in filenames, so this is a real, constructible primitive.
        shell: pwsh
        env:
          USERPROFILE: ${{ runner.temp }}\cyclaw-smoke-home-adversarial
        run: |
          New-Item -ItemType Directory -Force -Path $env:USERPROFILE | Out-Null

          # A real directory whose name is itself an injection payload -- the
          # exact class this finding is about, not a synthetic string. Only
          # gate.py needs to exist for Install-CyClaw.ps1's
          # -RepoPath validation to pass. The payload must be a LEGAL single
          # NTFS path component: no <>:"/\|?* (reserved chars). A previous
          # version embedded a quoted absolute Windows path (drive letter +
          # backslashes + quotes) inside the payload, which is illegal in a
          # single filename component -- New-Item would fail before
          # Install-CyClaw.ps1 was ever invoked, and continue-on-error masked
          # that setup failure, leaving Assert-SafeRepoPath untested (caught
          # by Codex review on PR #757). $, (, and ) are NTFS-legal, so
          # 'evil$(Get-Date)' both creates cleanly and still contains the
          # exact character class ($) Assert-SafeRepoPath rejects.
          $EvilDir = Join-Path "${{ runner.temp }}" 'evil$(Get-Date)'
          Copy-Item "$env:GITHUB_WORKSPACE\gate.py" (Join-Path $EvilDir "gate.py")

          $threw = $false
          try {
            & "$env:GITHUB_WORKSPACE\powershell\Install-CyClaw.ps1" -RepoPath $EvilDir -SkipPythonDeps
          } catch {
            $threw = $true
            if ($_.Exception.Message -notmatch "refusing") {
              Write-Host "[smoke-adversarial] FAIL: installer failed for an unexpected reason: $($_.Exception.Message)"
              exit 1
            }
          }
          if (-not $threw) {
            Write-Host "[smoke-adversarial] FAIL: installer accepted a -RepoPath containing shell metacharacters (should have refused)"
            exit 1
          }
          if (Test-Path (Join-Path $env:USERPROFILE ".CyClaw\bin\cyclaw.cmd")) {
            Write-Host "[smoke-adversarial] FAIL: shim was written despite the rejected path"
            exit 1
          }
          Write-Host "[smoke-adversarial] Install-CyClaw.ps1 correctly refused a shell-metacharacter repo path"

      - name: macOS install-script adversarial --repo-path smoke test
        if: runner.os == 'macOS'
        # Regression test for a confirmed command-injection finding
        # (docs/audits/2026-08-02-cyclaw-sandbox-macos-harness-and-guardrails.md):
        # install-cyclaw.sh writes the cyclaw shim and the rc-file cyclaw()
        # function via an unquoted, unescaped interpolation of the resolved
        # --repo-path -- a directory name containing a shell metacharacter used
        # to inject live code into both generated files, executed on every
        # subsequent shim/shell invocation. The benign-path smoke test above
        # provides zero coverage against this class of input by construction
        # (it always passes a clean checkout path), so this is a second,
        # deliberately adversarial step, not a duplicate.
        shell: bash
        env:
          HOME: ${{ runner.temp }}/cyclaw-smoke-home-adversarial
        run: |
          set -uo pipefail  # not -e: the installer's own rejection (exit 1) is the expected outcome
          mkdir -p "$HOME"
          fail() { echo "[smoke-adversarial] FAIL: $1" >&2; exit 1; }

          # A real directory whose name is itself an injection payload -- the
          # exact class this finding is about, not a synthetic string. Only
          # gate.py needs to exist for install-cyclaw.sh's --repo-path
          # validation to pass (see the check at repo-resolution time); a full
          # copy of $GITHUB_WORKSPACE is unnecessary and needlessly slow.
          EVIL_DIR="${{ runner.temp }}/evil\$(touch ${{ runner.temp }}/PWNED_VIA_INSTALLER)"
          mkdir -p "$EVIL_DIR"
          cp "$GITHUB_WORKSPACE/gate.py" "$EVIL_DIR/gate.py"

          if bash macos/install-cyclaw.sh --repo-path "$EVIL_DIR" --skip-python-deps < /dev/null 2>/tmp/adversarial_install.log; then
            fail "installer accepted a --repo-path containing shell metacharacters (should have refused)"
          fi
          grep -qi "refusing" /tmp/adversarial_install.log \
            || fail "installer failed for an unexpected reason (expected the metachar-rejection message): $(cat /tmp/adversarial_install.log)"
          [ -f "${{ runner.temp }}/PWNED_VIA_INSTALLER" ] && fail "injected command actually ran"
          [ ! -e "$HOME/.CyClaw/bin/cyclaw" ] || fail "shim was written despite the rejected path"
          echo "[smoke-adversarial] install-cyclaw.sh correctly refused a shell-metacharacter repo path"

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          # Key the pip download cache to the dependency manifests. When these are
          # unchanged, pip restores wheels (incl. the heavy torch/chromadb/
          # sentence-transformers downloads) from cache instead of re-fetching --
          # the biggest win on the slower windows-latest leg. Conservative: this
          # only caches the pip download dir, so a stale/missing cache degrades to
          # a normal install, never a broken one.
          cache-dependency-path: |
            requirements.txt
            requirements-test.txt
            constraints.txt
            pyproject.toml

      - name: Upgrade pip (CVE mitigation for CI env only)
        shell: bash
        run: |
          python -m pip install --upgrade "pip==26.1.2"

      - name: Cache torch CPU wheel
        if: runner.os != 'macOS'
        # Torch (~2GB) is pinned infrequently; caching its wheel separately means
        # a requirements.txt or constraints.txt change only busts the lighter
        # runtime-deps cache (via the setup-python pip cache), not this one.
        # The wheel is downloaded once per (os, pin) pair and reused on every
        # subsequent run until the pin changes in pyproject.toml. macOS skips
        # this cache entirely -- see the macOS-specific install step below.
        # The torch==2.13.0+cpu pin (and this cache key) is duplicated in
        # pip-audit.yml, copilot-setup-steps.yml, and devskim.yml -- grep for
        # the version string under .github/workflows/ before bumping it here.
        id: torch-wheel-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: .torch-wheels
          key: torch-cpu-2.13.0-${{ runner.os }}-py312
          restore-keys: |
            torch-cpu-2.13.0-${{ runner.os }}-

      - name: Download torch wheel (cache miss only)
        if: runner.os != 'macOS' && steps.torch-wheel-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          # CVE-2025-32434 (weights_only=True RCE bypass) was fixed in torch 2.6.0.
          # 2.13.0 is within the patched range. --no-deps avoids pulling the default
          # CUDA build; only the CPU wheel is fetched and cached.
          pip download "torch==2.13.0+cpu" \
            --index-url https://download.pytorch.org/whl/cpu \
            --no-deps -d .torch-wheels

      - name: Install deps (torch CPU + project + test tools)
        if: runner.os != 'macOS'
        shell: bash
        run: |
          # Install torch from the per-version local wheel (no network fetch when
          # cache hits). --no-index / --find-links keeps pip from querying the
          # PyTorch index on cache-hit runs, speeding up the step significantly.
          pip install --no-index --no-deps --find-links .torch-wheels "torch==2.13.0+cpu"
          pip install -r requirements.txt -r requirements-test.txt -c constraints.txt

      - name: Install deps (macOS -- plain torch, no +cpu suffix)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          # Confirmed via this matrix leg's first run: torch==2.13.0+cpu 404s on
          # https://download.pytorch.org/whl/cpu for macOS -- that index lists
          # plain (non "+cpu"-suffixed) versions there instead, because Apple
          # Silicon has no separate CPU/CUDA build to disambiguate (this was
          # flagged as an unverified gap in the macOS launcher docs; this is the
          # fix it named). requirements.txt/constraints.txt both hardcode the
          # +cpu pin by design (dependency-confusion-proof reproducibility on
          # Linux/Windows), so this leg installs plain torch directly, then
          # everything else from a requirements.txt copy with the torch/
          # extra-index-url lines stripped -- so pip never tries to reconcile
          # the installed plain build against the +cpu-suffixed pin -- and a
          # constraints.txt copy that keeps torch pinned minus the +cpu suffix
          # (dropping the line entirely leaves torch unconstrained; see
          # macos/install-cyclaw.sh's Darwin branch for the reinstall case).
          pip install "torch==2.13.0"
          grep -v -e '^torch==' -e '^--extra-index-url https://download.pytorch.org' requirements.txt > /tmp/requirements-macos.txt
          sed 's/^\(torch==[0-9][0-9.]*\)+cpu$/\1/' constraints.txt > /tmp/constraints-macos.txt
          pip install -r /tmp/requirements-macos.txt -r requirements-test.txt -c /tmp/constraints-macos.txt

      - name: Prepare hermetic test env
        shell: bash
        run: |
          # data/corpus/cyclaw_overview.md is committed — the RAG smoke test indexes it.
          # Minimal soul.md + env var for gate tests. No prod secrets.
          mkdir -p data/personality index logs
          echo '# Soul' > data/personality/soul.md
          echo 'GROK_API_KEY=dummy' >> "$GITHUB_ENV"

      - name: macOS real fsconnect policy + install-to-read integration
        if: runner.os == 'macOS'
        shell: bash
        run: |
          # Verbose output is intentional: the full-suite `-q` progress alone
          # cannot show whether a host-dependent APFS probe ran or skipped.
          pytest -vv -rs \
            tests/test_fsconnect_macos_real.py \
            tests/test_macos_fsconnect_setup.py::test_installer_enabled_profile_lists_stats_reads_and_dry_runs_writes

      - name: macOS real launchd scheduler integration
        if: runner.os == 'macOS'
        shell: bash
        run: |
          # Same reasoning as the fsconnect step above: the full-suite `-q`
          # progress alone cannot show whether the real launchctl probe ran
          # or was silently skipped.
          pytest -vv -rs tests/test_launchd_scheduler_macos_real.py

      - name: Windows native fsconnect handle-authority integration
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # Keep the native security matrix visible by test name. The full
          # suite runs it again; this early gate makes a platform skip or a
          # junction/handle regression impossible to hide in quiet progress.
          pytest -vv -rs tests/test_fsconnect_pathsafe_windows.py

      - name: Cache embeddings model (sentence-transformers)
        # retrieval/embeddings.py fetches models.embeddings.model (all-MiniLM-L6-v2,
        # ~90MB) from HuggingFace on first use into cache_dir (config.yaml:
        # models.embeddings.cache_dir, ".emb_cache" relative to repo root) -- no
        # prior step seeds it, so every run re-downloads it fresh. That's the one
        # real external network fetch this job's RAG smoke step depends on;
        # caching it removes the repeat download and narrows exposure to the kind
        # of transient fetch failure that surfaced as a one-off CyClaw Conda CI
        # red X during a multi-PR merge landing on 2026-07-27 (a rerun and the
        # very next push both came back clean, pointing at a transient fetch, not
        # a code regression).
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: .emb_cache
          key: emb-model-${{ hashFiles('config.yaml') }}
          restore-keys: |
            emb-model-

      - name: RAG Query Smoke (ChromaDB + BM25 + RRF)
        shell: bash
        run: |
          # Validates full retrieval path against committed corpus.
          # Fails CI if min_score gate or hybrid fusion broken.
          # (Unit tests + coverage run in the next step.)
          python -m tests.ci_rag_smoke

      - name: macOS live gateway API smoke
        # macOS is CyClaw's stated primary platform (CLAUDE.md). This step
        # boots mock-ollama + gate.py, then runs the Darwin twin of
        # windows-smoke.ps1 (macos-smoke.sh) for the same live-API contract. Isolated CYCLAW_HOME so the session residue
        # (title macos-smoke) never touches the runner's real home.
        if: runner.os == 'macOS'
        shell: bash
        env:
          GROK_API_KEY: dummy
          CYCLAW_API_KEY: smoke-test-key-ci
          CYCLAW_HOME: ${{ runner.temp }}/cyclaw-runtime-home
          PORT: '8787'
          PYTHON: python
        run: |
          set -uo pipefail
          RUNTIME_DIR="$RUNNER_TEMP/cyclaw-macos-live-smoke"
          mkdir -p "$RUNTIME_DIR" "$CYCLAW_HOME"
          PIDS=()
          LOGS=()

          # SIGTERM, wait for the process to actually exit, escalate to
          # SIGKILL if it lingers -- same pattern as
          # .claude/skills/CyClaw-Sandbox/verify.sh's _stop_pid, so no
          # process here leaves its port bound for a later CI step.
          stop_pid() {
            local pid="$1"
            [ -n "$pid" ] || return 0
            kill "$pid" 2>/dev/null || true
            for _ in $(seq 1 20); do
              kill -0 "$pid" 2>/dev/null || break
              sleep 0.5
            done
            kill -9 "$pid" 2>/dev/null || true
          }
          cleanup() {
            for pid in "${PIDS[@]:-}"; do
              stop_pid "$pid"
            done
          }
          trap cleanup EXIT

          # Sets LAST_PID rather than `echo`ing it: a caller capturing the
          # return value via `X=$(start_process ...)` would run this whole
          # function in a command-substitution SUBSHELL, so the PIDS/LOGS
          # array appends below would vanish with that subshell on return
          # and cleanup() would have nothing to kill. Call as a plain
          # statement and read $LAST_PID immediately after.
          LAST_PID=""
          start_process() {
            local name="$1"; shift
            local stdout="$RUNTIME_DIR/$name.stdout.log"
            local stderr="$RUNTIME_DIR/$name.stderr.log"
            "$@" >"$stdout" 2>"$stderr" &
            LAST_PID=$!
            PIDS+=("$LAST_PID")
            LOGS+=("$stdout" "$stderr")
          }

          wait_for_endpoint() {
            local name="$1" url="$2" pid="$3"
            for _ in $(seq 1 60); do
              if ! kill -0 "$pid" 2>/dev/null; then
                echo "FATAL: $name exited before becoming ready" >&2
                return 1
              fi
              if curl -sf -o /dev/null "$url"; then
                return 0
              fi
              sleep 0.5
            done
            echo "FATAL: $name did not become ready at $url" >&2
            return 1
          }

          on_failure() {
            for log in "${LOGS[@]:-}"; do
              if [ -f "$log" ]; then
                echo "===== $log ====="
                cat "$log"
              fi
            done
          }

          start_process "mock-ollama" python "$GITHUB_WORKSPACE/.claude/skills/CyClaw-Sandbox/mock_ollama.py" \
            --host 127.0.0.1 --port 11434 --model qwen3.8:27b-mlx
          MOCK_PID="$LAST_PID"
          wait_for_endpoint "mock Ollama" "http://127.0.0.1:11434/api/tags" "$MOCK_PID" || { on_failure; exit 1; }

          start_process "gateway" python -m uvicorn gate:app --host 127.0.0.1 --port 8787
          GATEWAY_PID="$LAST_PID"
          wait_for_endpoint "gateway" "http://127.0.0.1:8787/health" "$GATEWAY_PID" || { on_failure; exit 1; }

          echo "=== CyClaw macOS live smoke (http://127.0.0.1:8787) ==="
          if ! bash "$GITHUB_WORKSPACE/.claude/skills/CyClaw-Sandbox/macos-smoke.sh"; then
            on_failure
            exit 1
          fi

      - name: Windows live gateway API smoke
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GROK_API_KEY: dummy
          CYCLAW_API_KEY: smoke-test-key-ci
          CYCLAW_HOME: ${{ runner.temp }}\cyclaw-runtime-home
        run: |
          $ErrorActionPreference = "Stop"
          $Python = (Get-Command python -CommandType Application | Select-Object -First 1).Source
          $Pwsh = (Get-Command pwsh -CommandType Application | Select-Object -First 1).Source
          $RuntimeDir = Join-Path $env:RUNNER_TEMP "cyclaw-windows-live-smoke"
          New-Item -ItemType Directory -Force -Path $RuntimeDir, $env:CYCLAW_HOME | Out-Null
          $script:Processes = @()
          $script:Logs = @()

          function Start-CyClawProcess {
            param(
              [string]$Name,
              [string[]]$Arguments,
              [string]$WorkingDirectory
            )
            $stdout = Join-Path $RuntimeDir "$Name.stdout.log"
            $stderr = Join-Path $RuntimeDir "$Name.stderr.log"
            $process = Start-Process -FilePath $Python -ArgumentList $Arguments `
              -WorkingDirectory $WorkingDirectory -WindowStyle Hidden -PassThru `
              -RedirectStandardOutput $stdout -RedirectStandardError $stderr
            $script:Processes += $process
            $script:Logs += $stdout, $stderr
            return $process
          }

          function Wait-CyClawEndpoint {
            param(
              [string]$Name,
              [string]$Uri,
              [System.Diagnostics.Process]$Process
            )
            for ($attempt = 0; $attempt -lt 60; $attempt++) {
              if ($Process.HasExited) {
                throw "$Name exited before becoming ready (exit $($Process.ExitCode))"
              }
              try {
                $response = Invoke-WebRequest -Uri $Uri -UseBasicParsing -TimeoutSec 2
                if ($response.StatusCode -eq 200) { return }
              }
              catch {
                # Expected while the loopback-only process is starting.
              }
              Start-Sleep -Milliseconds 500
            }
            throw "$Name did not become ready at $Uri"
          }

          try {
            $mock = Start-CyClawProcess -Name "mock-ollama" -WorkingDirectory $RuntimeDir -Arguments @(
              (Join-Path $env:GITHUB_WORKSPACE ".claude\skills\CyClaw-Sandbox\mock_ollama.py"),
              "--host", "127.0.0.1", "--port", "11434", "--model", "qwen3.8:27b-mlx"
            )
            Wait-CyClawEndpoint -Name "mock Ollama" -Uri "http://127.0.0.1:11434/api/tags" -Process $mock

            $gateway = Start-CyClawProcess -Name "gateway" -WorkingDirectory $env:GITHUB_WORKSPACE -Arguments @(
              "-m", "uvicorn", "gate:app", "--host", "127.0.0.1", "--port", "8787"
            )
            Wait-CyClawEndpoint -Name "gateway" -Uri "http://127.0.0.1:8787/health" -Process $gateway

            $smokeStdout = Join-Path $RuntimeDir "windows-smoke.stdout.log"
            $smokeStderr = Join-Path $RuntimeDir "windows-smoke.stderr.log"
            $smoke = Start-Process -FilePath $Pwsh -ArgumentList @(
              "-NoLogo", "-NoProfile", "-File",
              (Join-Path $env:GITHUB_WORKSPACE ".claude\skills\CyClaw-Sandbox\windows-smoke.ps1")
            ) -WorkingDirectory $env:GITHUB_WORKSPACE -WindowStyle Hidden -Wait -PassThru `
              -RedirectStandardOutput $smokeStdout -RedirectStandardError $smokeStderr
            Get-Content -LiteralPath $smokeStdout
            if ($smoke.ExitCode -ne 0) {
              if (Test-Path -LiteralPath $smokeStderr) { Get-Content -LiteralPath $smokeStderr }
              throw "windows-smoke.ps1 failed with exit $($smoke.ExitCode)"
            }
          }
          catch {
            foreach ($log in $script:Logs) {
              if (Test-Path -LiteralPath $log) {
                Write-Host "===== $log ====="
                Get-Content -LiteralPath $log
              }
            }
            throw
          }
          finally {
            foreach ($process in $script:Processes) {
              if ($null -ne $process -and -not $process.HasExited) {
                Stop-Process -Id $process.Id -Force -ErrorAction SilentlyContinue
              }
            }
          }

      - name: Run tests + coverage
        shell: bash
        run: |
          # tests/ glob auto-discovers all test_*.py files — new test files are
          # picked up without a manual ci.yml edit. ci_rag_smoke.py is excluded
          # by pytest's default discovery pattern (it doesn't match test_*.py)
          # and runs separately in the RAG smoke step above.
          pytest tests/ \
            -q -rs --tb=short \
            --cov=gate \
            --cov=gate_ops \
            --cov=gate_auth \
            --cov=gate_memory \
            --cov=memory \
            --cov=utils.agent_identity \
            --cov=utils.repo_paths \
            --cov=utils.authn \
            --cov=utils.authn_store \
            --cov=utils.authn_manager \
            --cov=utils.authn_cli \
            --cov=utils.gen_cert \
            --cov=utils.sanitizer \
            --cov=utils.telemetry_kill \
            --cov=utils.onnx_telemetry \
            --cov=utils.config_validation \
            --cov=utils.errors \
            --cov=utils.logger \
            --cov=utils.numbat_emitter \
            --cov=utils.numbat_cel \
            --cov=utils.spend \
            --cov=utils.sequence_detect \
            --cov=utils.mcp_manifest \
            --cov=utils.personality \
            --cov=utils.personality_db \
            --cov=utils.ratelimit \
            --cov=utils.health \
            --cov=utils.ops_runner \
            --cov=utils.external_pre_hook \
            --cov=utils.guardrail_bridge \
            --cov=utils.selftest \
            --cov=utils.launchd_plist \
            --cov=utils.win_schtasks \
            --cov=utils.endpoint_trust \
            --cov=utils.tool_broker \
            --cov=llm.client \
            --cov=graph \
            --cov=retrieval.embeddings \
            --cov=retrieval.hybrid_search \
            --cov=retrieval.indexer \
            --cov=retrieval.stemmer \
            --cov=retrieval.vector_store \
            --cov=retrieval.clear_cache \
            --cov=retrieval.results \
            --cov=mcp_hybrid_server \
            --cov=metrics \
            --cov=sync.selftest \
            --cov=sync.runner \
            --cov=sync.config \
            --cov=sync.filters \
            --cov=sync.cli \
            --cov=sync.scheduler \
            --cov=agentic \
            --cov=guardrails \
            --cov=telegram \
            --cov=opentweet \
            --cov=schemas \
            --cov-report=xml \
            --cov-report=html \
            --cov-report=term-missing

      - name: Upload Coverage Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: coverage-${{ matrix.os }}
          path: |
            htmlcov/
            coverage.xml
          retention-days: 30

      - name: Final status
        run: echo "CI workflow completed on ${{ matrix.os }} (reproducible gate passed)"

  # Real-socket smoke for LocalLLMClient against a mock OpenAI-compatible
  # Ollama endpoint. tests/test_client.py already covers every error-mapping/
  # retry branch via a monkeypatched httpx.Client.post (real Request/Response
  # objects, no real socket) -- this exercises the actual wire protocol (JSON
  # bytes over a real loopback TCP connection) instead. No torch/chromadb/
  # sentence-transformers needed: llm/client.py only imports httpx + yaml.
  ollama-mock-smoke:
    name: ollama-mock-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          # This job installs an ad-hoc pinned set (below) rather than
          # requirements.txt, but every version it resolves comes from
          # constraints.txt -- so that plus the workflow file (which names the
          # packages) is the correct key. Download-cache only: a stale or
          # missing entry degrades to a normal install, never a broken one.
          cache-dependency-path: |
            constraints.txt
            .github/workflows/ci.yml

      - name: Install ollama-mock-smoke test deps (no torch/chromadb needed)
        # pytest always imports tests/conftest.py regardless of which test file
        # is targeted, and conftest.py imports retrieval.hybrid_search -> rank_bm25
        # at module load time -- rank-bm25/nltk/numpy are required even though
        # this specific test file never touches retrieval. Same minimal set
        # postgres-backend already installs for the identical reason.
        # (this exact install line is duplicated in the sibling job below/above -- both are small, single-purpose smoke lanes; a real shared step would need a composite action for what is currently only 2 call sites, which this repo's own minimal-abstraction convention argues against for now)
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          pip install -c constraints.txt httpx pyyaml pytest rank-bm25 nltk numpy

      - name: Run LocalLLMClient real-socket tests
        run: pytest tests/test_llm_client_ollama.py -v --tb=short

      - name: Final status
        run: echo "Ollama mock verification completed (llm.client OpenAI-compatible path, real socket)"

  # Real-socket, real subprocess (deterministic fake gh), real local-git-clone smoke for real-repo-run
  # wiring. tests/test_agentic_real_repo_run_cli.py already covers this
  # command's contract exhaustively, but mocks all three of its outer
  # boundaries in Python (agentic.context.run_read, repo_workspace.run_read,
  # LocalProposerClient.invoke) -- none of them ever touch a real socket, a
  # real gh subprocess, or a real timeout. An emulated rehearsal found two
  # real defects in exactly that gap (planner timeout never reaching the
  # model client; ops_runner's ceiling not scaling with it once it did) that
  # were invisible to every existing test for the same reason. See
  # docs/agentic/DEFERRED_WORK.md D1 for the full design rationale. Same
  # no-torch/chromadb reasoning as ollama-mock-smoke above: agentic/ never
  # imports retrieval/gate/graph (invariant I6).
  real-repo-run-smoke:
    name: real-repo-run-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          # Same key as the sibling ollama-mock-smoke job above, for the same
          # reason: ad-hoc package list in this file, versions from constraints.
          cache-dependency-path: |
            constraints.txt
            .github/workflows/ci.yml

      - name: Install real-repo-run-smoke test deps (no torch/chromadb needed)
        # Same minimal set ollama-mock-smoke installs, for the same reason
        # (conftest.py imports retrieval.hybrid_search -> rank_bm25 at module
        # load time regardless of which test file is targeted). Verified by
        # reading every module on the --repo (local-model) real-repo-run call
        # path's own import list -- none needs anything beyond this set; the
        # cloud-provider path (langchain_core et al.) is lazily imported only
        # inside cmd_real_repo_run's `if provider:` branch, which this smoke
        # test's --repo (no --provider) invocation never reaches.
        # (this exact install line is duplicated in the sibling job below/above -- both are small, single-purpose smoke lanes; a real shared step would need a composite action for what is currently only 2 call sites, which this repo's own minimal-abstraction convention argues against for now)
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          pip install -c constraints.txt httpx pyyaml pytest rank-bm25 nltk numpy

      - name: Run real-repo-run smoke test
        run: pytest tests/test_agentic_real_repo_run_smoke.py -v --tb=short

      - name: Final status
        run: echo "real-repo-run smoke completed (real socket/subprocess/local clone; fake gh; no live GitHub)"

  # Optional Deep Agents contract check. This deliberately installs only the
  # lightweight agentic dependency set; default runtime CI remains offline-first.
  deepagents-harness:
    name: deepagents-harness (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          # The heaviest uncached install in the file: langgraph + langchain-core
          # + deepagents + langchain-openai + langchain-xai and their transitive
          # trees, re-downloaded on every run. Same constraints-plus-workflow key
          # as the two smoke jobs above.
          cache-dependency-path: |
            constraints.txt
            .github/workflows/ci.yml

      - name: Install optional Deep Agents harness dependencies
        shell: bash
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          pip install -c constraints.txt pyyaml httpx langgraph langchain-core pytest pytest-asyncio rank-bm25
          pip install -c constraints.txt deepagents langchain-openai langchain-xai
          python -m pip check

      - name: Verify Deep Agents HITL and fixture runner
        shell: bash
        run: |
          pytest tests/test_agentic_harness_phase679.py tests/test_agentic_deepagent_optional.py \
            tests/test_agentic_cloud_chat_model_wire_format.py -q --tb=short

      - name: Audit optional Deep Agents dependencies (freeze, not hand-written)
        if: runner.os == 'Linux'
        # Captures the versions actually resolved/installed above rather than a
        # hardcoded string -- a hand-maintained version list here would be the
        # same class of silent-drift risk dep-guard's D8 check exists to catch
        # for torch (a bumped constraints.txt pin wouldn't update a hardcoded
        # copy elsewhere, so pip-audit would keep auditing a version nobody runs).
        # The alternation was previously '(deepagents|langchain|langchain-openai)=='
        # which required '==' immediately after the token, so every other
        # langchain-* distribution -- langchain-anthropic and langchain-google-genai
        # (both MANDATORY deps of deepagents), plus langsmith and now langchain-xai
        # -- was installed but never audited.
        shell: bash
        env:
          DEEPAGENTS_FREEZE: deepagents-requirements.txt
        run: |
          pip install pip-audit==2.10.1
          pip freeze | grep -Ei '^(deepagents|langchain|langchain-[a-z0-9-]+|langsmith)==' > "$DEEPAGENTS_FREEZE"
          cat "$DEEPAGENTS_FREEZE"
          pip-audit -r "$DEEPAGENTS_FREEZE"

  # ==============================================================================
  # Postgres backend job (opt-in DB paths)
  # Exercises the LIVE psycopg/pgvector connect+execute paths that the default
  # matrix can only skip: the soul DB over Postgres, the rate limiter over
  # Postgres, the pgvector vector store, and authn. Ubuntu-only — service containers do
  # not run on the windows-latest leg. Uses pgvector/pgvector:pg16 (a superset of
  # postgres:16 that ships the `vector` extension). Lean deps: torch / chromadb /
  # sentence-transformers / fastapi stay lazy and are not needed; the job installs
  # only the import-time deps the shared tests/conftest.py pulls in (rank-bm25 + nltk
  # via retrieval.hybrid_search) plus the Postgres drivers. sslmode=disable because
  # the service is a trusted same-host container.
  # ==============================================================================
  postgres-backend:
    name: postgres-backend
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        # Digest-pinned (resolved 2026-07-21), same rationale as the uv image
        # pin in the Dockerfile: a re-tagged/compromised :pg16 image would
        # otherwise silently enter this job. Trade-off: this image stops
        # picking up Postgres security patches automatically -- re-pin the
        # digest periodically (`docker manifest inspect pgvector/pgvector:pg16`
        # or the registry API) rather than letting it go stale indefinitely.
        image: pgvector/pgvector:pg16@sha256:1d533553fefe4f12e5d80c7b80622ba0c382abb5758856f52983d8789179f0fb
        env:
          POSTGRES_USER: cyclaw
          POSTGRES_PASSWORD: cyclaw
          POSTGRES_DB: cyclaw
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U cyclaw"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      # Loopback DSN for the ephemeral CI service container, not debug code.
      CYCLAW_DB_URL: postgresql://cyclaw:cyclaw@127.0.0.1:5432/cyclaw  # DevSkim: ignore DS162092
      CYCLAW_DB_SSLMODE: disable
      GROK_API_KEY: dummy
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: |
            constraints.txt
            pyproject.toml

      - name: Upgrade pip (CVE mitigation for CI env only)
        run: python -m pip install --upgrade "pip==26.1.2"

      - name: Install Postgres-backend deps
        run: |
          # Postgres drivers + the import-time deps tests/conftest.py needs
          # (retrieval.hybrid_search → rank-bm25, nltk; numpy backs rank-bm25).
          # Constraints pin psycopg/psycopg-binary 3.2.13, pgvector 0.4.2, and the
          # rest to the project versions. PorterStemmer needs no nltk_data download.
          pip install -c constraints.txt 'psycopg[binary]' pgvector pyyaml rank-bm25 nltk numpy pytest

      - name: Run live Postgres / pgvector backend tests
        run: |
          pytest \
            tests/test_personality_postgres.py \
            tests/test_ratelimit_postgres.py \
            tests/test_pgvector_store.py \
            tests/test_personality_db.py \
            tests/test_authn_postgres.py \
            -q --tb=short

      - name: Final status
        run: echo "Postgres backend tests passed (soul DB + rate limiter + pgvector + authn)"

  # ==============================================================================
  # Skill verification matrix (new in feature/CyClaw-Agent)
  # Runs every .claude/skills/*/verify.sh and smoke.sh in parallel.
  # This enforces that all skills (including new agentic/memory ones) stay
  # healthy and respect the governance invariants.
  # ==============================================================================
  # A matrix cannot reference a step output from its own job (the matrix is
  # evaluated before any step runs). Correct pattern: a discover job emits the
  # skill list as an output, then the matrix job consumes it via `needs`.
  discover-skills:
    name: discover-skills
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      skills: ${{ steps.discover.outputs.skills }}
      has_skills: ${{ steps.discover.outputs.has_skills }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Discover skills and dependency profiles
        id: discover
        run: |
          set -o pipefail
          SKILLS=$(find .claude/skills -maxdepth 1 -mindepth 1 -type d | sort | while read -r d; do
            if [ -f "$d/verify.sh" ] || [ -f "$d/smoke.sh" ]; then
              skill=$(basename "$d")
              case "$skill" in
                CyClaw-Sandbox|index-doctor) profile=heavy ;;
                config-guard|doc-sync|injection-redteam) profile=yaml ;;
                cyclaw-advisor|cyclaw-gotchas|dep-guard|invariant-guard|otel-hardening|verify-deps) profile=stdlib ;;
                *)
                  # Unknown skills must fail closed: a silent stdlib default can
                  # skip-green when the verifier needs PyYAML/runtime the stdlib
                  # job does not install. Add an explicit heavy|yaml|stdlib arm.
                  echo "error: unclassified skill '$skill' — add an explicit profile (heavy|yaml|stdlib) in discover-skills" >&2
                  exit 1
                  ;;
              esac
              jq -cn --arg skill "$skill" --arg profile "$profile" \
                '{skill: $skill, profile: $profile}'
            fi
          done | jq -sc '.')
          echo "skills=$SKILLS" >> "$GITHUB_OUTPUT"
          if [ "$SKILLS" = "[]" ]; then
            echo "has_skills=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_skills=true" >> "$GITHUB_OUTPUT"
          fi
          echo "Discovered skills: $SKILLS"

  verify-skills:
    name: verify-skills (${{ matrix.skill }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Deliberately runs IN PARALLEL with `test` (needs only discover-skills).
    # A `needs: test` serialization was tried (2026-07-17) to guarantee this
    # matrix always finds a warm torch-wheel cache, and reverted the same day
    # on measured data: workflow wall-clock went from ~4.2 min to ~8.9 min
    # (all 9 matrix jobs were created the exact second the slower
    # windows-latest test leg finished, then added ~3.8 min of their own),
    # while on warm-cache runs — which is nearly every run — each matrix job
    # restores the wheel independently in 1-4 seconds anyway. The only thing
    # serialization bought was avoiding parallel re-downloads on a cold key
    # (torch pin bump / 7-day eviction), which costs bandwidth GitHub-hosted
    # runners don't meter, not wall time. Do not re-add `needs: test` without
    # new measurements showing the trade has changed.
    needs: discover-skills
    # Skip cleanly when no skill ships a verify/smoke script (empty matrix).
    if: needs.discover-skills.outputs.has_skills == 'true'
    # The scripts have explicit dependency profiles. Only CyClaw-Sandbox and
    # index-doctor need the full retrieval/runtime stack; config-guard, doc-sync,
    # and injection-redteam need PyYAML; the remaining verifiers are stdlib/shell.
    # The matrix remains NON-BLOCKING (continue-on-error):
    # the authoritative pass/fail gate is the `test` job's unit suite + RAG smoke.
    # This surfaces runtime regressions as a visible signal without gating merges
    # on environment-heavy e2e that can be flaky on shared runners.
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover-skills.outputs.skills) }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12 (stdlib profile)
        if: matrix.profile == 'stdlib'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'

      - name: Setup Python 3.12 (cached dependency profiles)
        if: matrix.profile != 'stdlib'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            requirements-test.txt
            constraints.txt
            pyproject.toml

      - name: Cache torch CPU wheel
        if: matrix.profile == 'heavy'
        # Same cache the `test` job uses (identical key): without it both heavy
        # skill legs re-download the ~2 GB torch wheel from the PyTorch index.
        id: torch-wheel-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: .torch-wheels
          key: torch-cpu-2.13.0-${{ runner.os }}-py312
          restore-keys: |
            torch-cpu-2.13.0-${{ runner.os }}-

      - name: Download torch wheel (cache miss only)
        if: matrix.profile == 'heavy' && steps.torch-wheel-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          pip download "torch==2.13.0+cpu" \
            --index-url https://download.pytorch.org/whl/cpu \
            --no-deps -d .torch-wheels

      - name: Install deps (torch CPU + project)
        if: matrix.profile == 'heavy'
        shell: bash
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          pip install --no-index --no-deps --find-links .torch-wheels "torch==2.13.0+cpu"
          pip install -r requirements.txt -c constraints.txt
          # Some skill scripts invoke `python3.12` explicitly; ensure it resolves.
          command -v python3.12 >/dev/null 2>&1 || sudo ln -sf "$(command -v python)" /usr/local/bin/python3.12

      - name: Install PyYAML only
        if: matrix.profile == 'yaml'
        shell: bash
        run: |
          python -m pip install --upgrade "pip==26.1.2"
          pip install -c constraints.txt pyyaml

      - name: Cache embeddings model (sentence-transformers)
        if: matrix.profile == 'heavy'
        # See ci.yml's identical step in the `test` job for the full rationale:
        # the CyClaw-Sandbox skill's verify.sh calls `python -m retrieval.indexer`,
        # which fetches models.embeddings.model (~90MB) from HuggingFace on a cold
        # cache. Without this step every matrix leg running that skill re-downloads
        # it fresh, unlike the `test` job and python-package-conda.yml, which both
        # already cache it under the identical key.
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: .emb_cache
          key: emb-model-${{ hashFiles('config.yaml') }}
          restore-keys: |
            emb-model-

      - name: Prepare hermetic env
        shell: bash
        run: |
          # NOTE: do not stub soul.md here. The committed data/personality/soul.md
          # is the real artifact, and skill verify scripts (e.g.
          # sandbox-runtime-verification/verify.sh) back it up, stub it for an
          # early stage, then restore the real file before launching the server.
          # Overwriting it with a 7-char '# Soul' stub here means verify.sh backs
          # up the stub and "restores" 7 chars, so /soul returns near-empty text
          # and the terminal.html emulation's non-empty soul assertion fails.
          mkdir -p data/personality index logs
          echo 'GROK_API_KEY=dummy' >> "$GITHUB_ENV"
          # CYCLAW_API_KEY needed for GET /soul smoke check (now auth-gated).
          # This is a non-secret CI smoke value — loopback-only, ephemeral.
          echo 'CYCLAW_API_KEY=smoke-test-key-ci' >> "$GITHUB_ENV"
          echo "Skill dependency profile: ${{ matrix.profile }}"

      - name: Run verify.sh if present
        if: hashFiles(format('.claude/skills/{0}/verify.sh', matrix.skill)) != ''
        # SKILL routed through env rather than interpolated into the run
        # script: matrix.skill is a directory name under .claude/skills/, and
        # while today those are maintainer-controlled, template-expanding it
        # directly into shell text would let a directory named with shell
        # metacharacters execute as code instead of just being read as a
        # string once it reaches "$SKILL" (zizmor template-injection).
        env:
          SKILL: ${{ matrix.skill }}
        run: |
          echo "Running verify for $SKILL"
          bash ".claude/skills/$SKILL/verify.sh"

      - name: Run smoke.sh if present
        if: hashFiles(format('.claude/skills/{0}/smoke.sh', matrix.skill)) != ''
        env:
          SKILL: ${{ matrix.skill }}
        run: |
          echo "Running smoke for $SKILL"
          bash ".claude/skills/$SKILL/smoke.sh"
