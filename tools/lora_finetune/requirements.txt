# Optional operator install for the LoRA fine-tune kit ONLY.
# CyClaw runtime (root requirements.txt / pyproject extras / Docker / conda)
# does NOT install these. Do not copy this file into a core install surface.
#
# Direct packages only — not a lockfile of Unsloth's GPU tree. OSV-Scanner
# v2 resolves requirements.txt transitives via deps.dev and, for this stack,
# materializes ancient wheels (aiohttp 3.9.5, pillow 9.5, setuptools 9.1,
# torch 2.9.1) that are not what a current `pip install unsloth` lands.
# The kit directory is therefore excluded from the repo OSV walk
# (--experimental-exclude=r:lora_finetune). Audit on the CUDA box after install:
#   pip-audit -r tools/lora_finetune/requirements.txt
#
# BLOCKED training profile (verified 2026-09-21): Unsloth 2026.9.4 and
# 2026.9.7 require transformers<=5.5.0 / trl<=0.24.0 / datasets<4.4,
# which conflict with the patched pins below. pip fails ResolutionImpossible.
# Do not bypass the conflict with --no-deps or an Unsloth-only install:
# transformers 5.5.0 is affected by CVE-2026-9856 (fixed in 5.10.0), and
# datasets 4.3.0 by CVE-2026-66007 (fixed in 5.0.1). Keep these pins until
# a compatible training stack is verified on CUDA. See README.md.

unsloth==2026.9.4
transformers==5.17.0
trl==1.13.0
datasets==5.0.1
accelerate==1.15.0
