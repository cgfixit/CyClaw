# CyClaw — constraints.txt  (generated 2026-06-13 from a verified Python 3.12 install)
# ... (keep most)
chromadb==1.5.10.dev133  # CVE-2026-45829 mitigated: CyClaw is localhost-only (127.0.0.1), offline-first, and explicitly sets trust_remote_code=False in embedding config + wrapper. Pre-release until upstream stable patch. Rebuild index after upgrade.
# ... rest of file unchanged for other pins